# 生产Renderer启动链验证结论

- 验证日期：2026-07-30
- Change：`connect-production-renderer-launch-chain`
- 构建宿主：macOS arm64
- 当前结论：生产Controller、Renderer注入、Codex/Pi控件与双进程监督已接通；Codex/Pi真实路由和reload恢复仍在最终复测

## 自动门禁

- `npm run check`：通过
- TypeScript/Vitest：79个测试文件通过、1个跳过；499项测试通过、4项跳过
- `tests/release`：7个文件、28项测试通过
- Rust Workspace fmt、Clippy和测试：通过
- Launcher：8个单元测试和3个CLI集成测试通过
- `connect-production-renderer-launch-chain` OpenSpec strict validation：通过
- `build-cross-platform-self-contained-release-staging` OpenSpec strict validation：通过

## 生产链事实

- Launcher使用随机`127.0.0.1` Electron Inspector端口，不启用Chromium remote debugging；
- 私有Node启动`app/desktop-controller.mjs`，Controller只在Control Session完成后输出`ready`；
- Controller通过同一Control Session安装Title Policy、reload主Renderer、确认ownership、安装Draft Prewarm Policy并执行生产IIFE；
- 生产Renderer只显示Codex和Pi，不包含Claude Code开发配置；
- 无参数Launcher把Pi作为生产Composer初始Agent传给Controller；Host对未携带外部transport carrier的创建固定使用Codex fallback，Pi仍由`codexhost/pi-native`显式路由；
- Controller安装失败曾真实触发Launcher关闭本次Desktop，证明未留下Stock UI继续运行；
- 当前新版真实会话中Desktop、Controller、Host Runtime和官方Codex app-server持续运行，Controller/Launcher日志为空；Host进程环境已确认`CODEXHOST_DEFAULT_AGENT=codex`。

## 真实macOS进度

- 用户已确认主Composer显示Codex/Pi Agent控件；
- 首轮测试发现旧组合中选择Codex仍进入Pi，根因是无参数Host fallback为Pi而Codex按规格保留官方Model；
- 新组合已改为“可见初始Pi + Host未标记fallback Codex + Pi显式transport”，自动测试与重新打包通过；
- 新版会话尚未观察到Pi Native Session，页面Codex回答仍等待用户确认；
- Pi真实首轮、页面locked状态和Renderer reload恢复尚未完成，不得声明生产Renderer Gate全部通过。

## 当前产物

- arm64 DMG：`build/release/0.0.0/macos-arm64/codexhost-0.0.0-macos-arm64.dmg`
- arm64 SHA-256：`a097d7cd5d40098539798b4ac180db3995e055a1d991afedf2b6bb93448755e1`
- x64 DMG：`build/release/0.0.0/macos-x64/codexhost-0.0.0-macos-x64.dmg`
- x64 SHA-256：`bf5a6be30928786c2a930452eacca04a6cbe6f894942d1aa4f88590d61509cfd`
- 两个Payload均严格包含十个文件，两个App均通过`codesign --verify --deep --strict`；x64仅完成Rosetta构建与Node smoke，未完成原生Intel Desktop Gate。

## 待关闭

1. 在新版真实会话中选择Codex创建Thread，确认回答来自官方Codex且未创建Pi Native Session。
2. 新建Thread选择Pi，确认真实Pi输出、同一页面Thread和Composer `locked`状态。
3. 触发Renderer reload，确认Title Policy、Draft Prewarm Policy和Codex/Pi控件恢复。
4. 完成上述证据后更新本记录并关闭任务6.2、6.3；Windows仍需独立真实宿主Gate。
