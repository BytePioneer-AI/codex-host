# 验证结论

## 结论

截至2026-07-29，本Change的Adapter、Protocol、Host和受控Renderer实现已完成。真实Claude Agent SDK Adapter Gate与真实Claude到Host JSON-RPC投影Gate通过。真实Codex Desktop Gate已证明Claude开发选项、Model-state绑定、标题隔离、Thread创建路由及首轮Turn关联；流式文本可见性、Desktop停止按钮和同Thread续轮仍等待人工结果确认，不在本记录中提前声明为通过。

该能力仍是显式启用的开发Gate。默认Renderer只启用Codex/Pi，默认Host只注册Pi，Pi仍是公开MVP的唯一外部Harness。

## 自动验证

| 验证项 | 结果 | 证据摘要 |
| --- | --- | --- |
| Claude Adapter Hermetic测试 | PASS | Fake Transport覆盖惰性启动、文本、冲突、完整Result分类、Cancel、Fault与close |
| 真实Claude Adapter Live测试 | PASS | 一轮文本成功、取消收敛、同Native Session续轮成功、有界close |
| 双Fake Host纵向测试 | PASS | Pi/Claude Thread隔离、早输出响应Gate、未注册token fail closed |
| 真实Claude Host投影Gate | PASS | transport token进入注册式Host路径并产生Agent Message delta与完成通知 |
| 全量TypeScript测试 | PASS | 59个测试文件通过，308项测试通过，2项显式Live测试在普通运行中按设计skip |
| Renderer build | PASS | browser bundle与受控Probe bundle构建成功 |
| OpenSpec strict validation | PASS | 11项spec/change全部通过 |
| `npm run check` | BLOCKED | Prettier通过后，本机缺少`cargo`，Rust阶段无法执行 |
| `npm run build` | BLOCKED | TypeScript与Renderer build通过，本机缺少`cargo`，Rust build无法执行 |

真实Live测试只在显式环境开关下运行。普通lint、typecheck、test和build不会启动Claude、读取用户配置或访问模型服务。

## 真实Desktop受控Gate

受控Gate使用显式Host与Renderer Claude开关。脱敏状态证明：

- Renderer enabled Agent列表为Codex、Pi、Claude Code；
- Renderer Adapter状态为`ready`，使用既有Model-state hook；
- Claude Composer提交后状态为`locked`；
- 标题策略对非Codex Harness执行跳过，未创建官方标题Turn；
- `thread/start`携带Claude transport分类，Host选择`claude-code`；
- 随后的`turn/start`与同一匿名create ordinal匹配；
- 真实Claude SDK子进程由当前Host启动；
- 同轮受控操作还产生了Codex与Pi的匹配路由，未观察到跨Harness误关联。

首次Gate发现Renderer配置注入表达式返回`window`会触发Electron结构化克隆错误。Runner已改为显式返回`null`，并让受控Desktop子进程在`--keep-desktop`下正确脱离Runner生命周期；重试后Renderer Gate进入`ready`。

以下Desktop验收仍待人工确认：

1. Claude文本在真实Codex Thread中开始流式显示；
2. 点击停止后Turn收敛为取消终态；
3. 同一Claude Thread能够接受并显示后续文本Turn。

在三项确认前，OpenSpec任务6.3保持未完成。

## 隐私边界

本记录不包含Prompt、Transcript、模型输出、完整Session/Thread/Turn/Item ID、账号信息、Token或本地绝对路径。真实Prompt和本地Gate原始证据只存在于Git忽略的`.codexhost/`目录。Claude SDK类型和原生消息解释仍只存在于`packages/adapters/claude-code`。
