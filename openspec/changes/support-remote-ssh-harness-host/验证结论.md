# 验证结论

验证日期：2026-08-22

## 本轮故障与修复

Mac 同时运行本地 codexhost Desktop 与托管 SSH Host 时，非交互 shell 的远端启动块会把 `CODEXHOST_HOST_RUNTIME_PATH`、`CODEXHOST_DATA_DIR` 等变量重新注入本地 Desktop 的 Codex Shim。由此依次出现两类启动失败：

1. 本地 npm Launcher 的更新元数据与远端复制的 Host Runtime 混用，触发 `npm platform package root does not own the Host Runtime`。
2. 本地 Host Runtime 继承远端 `CODEXHOST_DATA_DIR`，与远端 listener 争用同一个 Mapping Store，触发 `Another codexhost process owns Mapping Store`。

修复在三个启动边界上保持同一约束：npm 入口只在检测到托管远端 shell 环境时移除远端启动变量并恢复本地数据目录；native Launcher 显式把本地数据目录传给 Desktop；Shim 在存在本地 Launcher 身份时优先选择本地 npm Node 与 Host Runtime，并清除远端数据目录。直接由 SSH 启动、没有本地 Launcher 身份的 listener 继续使用远端 runtime 与 `~/.codexhost/remote/data`。Host Runtime 的更新协调器还会校验实际 runtime 是否归属于 npm package root，路径不匹配时不启动 npm 更新流程。

## 自动化门禁

- Windows 聚焦回归覆盖 Host Runtime 路径归属、npm 入口环境隔离、Launcher 数据目录传播、Platform 启动环境替换和 Shim 最终路径选择，均通过。macOS 还实际启动 `/usr/bin/env` 子进程验证：托管远端 profile 存在时，子进程只看到本地 data/runtime，远端 marker、install dir、data/runtime 均未泄漏。
- Windows Shim 集成回归 10/10 通过；macOS Shim 集成回归 17/17 通过。
- Windows `npm run check:rust` 通过：rustfmt、整个 workspace 的 Clippy `-D warnings`、单元测试、集成测试和文档测试均通过。
- TypeScript build、Prettier、ESLint、依赖边界、typecheck 与 `git diff --check` 通过。
- macOS ARM64 使用实际部署的 Node.js 24.16.0 运行 `packages/host-runtime/test/remote-host-install.test.ts`，13/13 通过。
- `npx --yes @fission-ai/openspec@1.10.0 validate support-remote-ssh-harness-host --strict` 通过。

Windows 当前系统 Node.js 为 24，整仓 TypeScript 门禁中 1060 个测试通过，5 个开发启动器用例按仓库 Node.js 22 保护失败，另有 1 个 fixture 因 Windows 符号链接权限失败。macOS/Homebrew Node.js 22 整仓 TypeScript 门禁中 1065 个测试通过、6 个按条件跳过、3 个失败：其中 2 个 Gate C RPC 用例立即单独重跑后通过；剩余 1 个远端安装 fixture 会把 Homebrew 动态链接的 Node 可执行文件复制到临时目录，复制品无法解析 `@rpath/libnode.127.dylib`。相同远端安装测试使用实际部署的 NVM Node.js 24.16.0 为 13/13 通过，且真实 listener Gate 也通过。以上平台 fixture 差异不计为整仓全绿，也不替代后面的实机证据。

## 候选构建与实机状态

- Windows x64 与 macOS ARM64 的最终候选版本均为 `0.2.6-remote-ssh.12`；Windows 全局 `codexhost` 与 Mac 全局 `codexhost` 均已安装该版本。
- Windows Codex Desktop `26.818.5229.0` 已由一次 stock Desktop 启动生成与当前应用内置 CLI 摘要匹配的本地 cache；`.12` 候选与全局 `codexhost inspect` 均成功。当前 stock Desktop 保持运行，因此未在本轮再次用 codexhost 接管 Windows Desktop。
- Mac 托管远端状态返回 `state=ready`、`issues=[]`，入口指向 `~/.codexhost/remote/runtime/0.2.6-remote-ssh.12`；manifest 中入口 SHA-256 为 `7646b7c04b494b580ba27bde4d3bb7ccf493bb8c23c051f0f2f199af245ac7df`。
- Mac 本地 codexhost Desktop 已由 `.12` 成功启动，Host chain ready，Launcher、Desktop Controller、Renderer 与 Host Runtime 均完成握手；最新日志没有 Mapping Store ownership、npm package root 或 Host Runtime 初始化错误。
- 本地 Mapping Store `~/.codexhost/mapping-store/store.lock` 与远端 Mapping Store `~/.codexhost/remote/data/mapping-store/store.lock` 同时存在，分别由两个不同 Node 进程持有；重启远端 `.12` listener 未影响本地锁持有者。
- Mac Claude Code 命令为 `/Users/moka/.local/bin/claude`，版本 `2.1.238`。本地 Desktop 日志已发现 Claude Code 条目与会话，连续 `codexhost/harness/inspect` 请求均以 `errorCode=null` 返回。

## 已覆盖回归

- 本地 npm 启动与远端 SSH 启动使用各自的 Node、Host Runtime、Mapping Store 与更新所有权；保留共享的 `CODEXHOST_CLAUDE_COMMAND`，不复制 Claude 凭据。
- 当前 Composer 的 scoped portal 没有 conversation ID 时，即使远程项目页祖先存在预热 conversation ID，也保持 `client-new-thread` draft 并允许 Agent 选择。
- draft 提交并绑定后，scoped portal 的 Thread ID 优先于仍留在 memo cache 中的 draft wrapper。
- 只有一个默认 `--listen unix://` 且没有 `--stdio` 的托管命令可以脱离 SSH bootstrap；stdio、重复 listener、自定义 listener 和管理命令保持前台生命周期。
- 新 manifest 可在旧包内 Shim 被移除后依据已安装摘要安全卸载；入口被修改时拒绝卸载；无摘要的旧 manifest 保持依赖源文件校验并 fail closed。

## 尚待用户界面验收

- Windows 当前 stock Codex Desktop 退出后，从全局 `codexhost` 启动 `.12`，重新连接 Mac SSH workspace。
- 在远程项目文件夹中新建任务，确认 Claude Code 可选；提交两轮上下文相关消息，并验证流式输出、取消和恢复。此项尚未由自动化或本轮本地 Mac 启动证据代替，因此 OpenSpec task 4.4 保持未完成。
