# 验证结论

验证日期：2026-08-22

## 自动化门禁

- Windows 针对性 TypeScript 回归：`npx vitest run --config tests/vitest.config.js packages/host-runtime/test/remote-host-install.test.ts packages/renderer-extension/test/versioned-renderer-adapter.test.ts`，结果为 2 个文件通过，26 个测试通过，1 个平台测试跳过。
- Windows Shim 回归：`cargo test --locked --package codexhost-shim --features codexhost-shim/test-utils`，结果为 3 个单元测试和 10 个集成测试通过。
- 类型检查：`npm run typecheck` 通过。
- OpenSpec：`openspec validate support-remote-ssh-harness-host --strict` 通过。
- macOS ARM64、Node.js 22 支持环境整仓门禁：`npm run check` 通过，包含格式、ESLint、依赖边界、类型检查、143 个 TypeScript 测试文件（1066 个测试通过、6 个按条件跳过）、Rustfmt、Clippy 和整个 Rust workspace 测试。

Windows 当前系统 Node.js 为 24，直接运行整仓门禁时，开发启动器的 Node 22 版本保护和需要 Windows 符号链接权限的 fixture 按预期失败；相同代码随后在仓库支持的 macOS/Node.js 22 环境完成了完整门禁。此环境差异不作为代码通过结论。

## 候选构建与远端安装

- Windows x64 候选：`0.2.6-remote-ssh.6`，已生成发布平台包、meta 包和按真实 tarball 布局安装的可启动候选目录。
- macOS ARM64 候选：`0.2.6-remote-ssh.6`，发布包构建通过。
- Mac SSH 开发机的托管配置已指向 `.6` runtime；`remote status` 返回 `ready` 且 `issues` 为空。
- Mac 托管入口与 `.6` 包内 Shim 的 SHA-256 相同，manifest 已记录该摘要。当前既有 listener 仍保持运行，只有重新连接远程工作区后才会由 `.6` 入口启动新 listener。

## 已覆盖回归

- 当前 Composer 的 scoped portal 没有 conversation ID 时，即使远程项目页祖先存在预热 conversation ID，也保持 `client-new-thread` draft 并允许 Agent 选择。
- draft 提交并绑定后，scoped portal 的 Thread ID 优先于仍留在 memo cache 中的 draft wrapper。
- 只有一个默认 `--listen unix://` 且没有 `--stdio` 的托管命令可以脱离 SSH bootstrap；stdio、重复 listener、自定义 listener 和管理命令保持前台生命周期。
- 新 manifest 可在旧包内 Shim 被移除后依据已安装摘要安全卸载；入口被修改时拒绝卸载；无摘要的旧 manifest 保持依赖源文件校验并 fail closed。

## 待实机验收

- 退出当前 Windows Codex Desktop，使用 `.6` 候选重新启动。
- 重新连接 Mac SSH workspace，使远端 listener 从 `.5` 进程切换为 `.6`。
- 在远程项目文件夹中新建任务，确认 Claude Code 可选；提交两轮上下文相关消息，并验证流式输出、取消和恢复。
