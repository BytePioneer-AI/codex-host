# codexhost 发布提交指南

本文用于指导 AI 完成 codexhost 版本发布。正式发布只通过 annotated Git Tag 触发 GitHub Actions；不要在本地执行 `npm publish` 或手动创建 GitHub Release。

## 发布前确认

- 用户已明确目标 SemVer，且该版本和对应 `v<version>` Tag 从未发布。
- 当前提交基于最新 `main`；不得覆盖、回退或提交无关的工作区改动。
- GitHub Actions 已配置有效的 npm 发布凭据。

版本渠道：

| 版本格式 | npm dist-tag | GitHub Release |
| --- | --- | --- |
| `*-test.*` | `test` | Prerelease |
| `*-alpha.*`、`*-beta.*`、`*-rc.*` | `next` | Prerelease |
| 稳定 SemVer | `latest` | 正式 Release |

## 操作步骤

1. 检查分支和工作区：

```powershell
git status --short --branch
git pull --ff-only
```

2. 同步 npm 与 Cargo 产品版本：

```powershell
npm run release:prepare -- <version>
```

该命令应只更新 `package.json`、`package-lock.json`、`Cargo.toml` 和 `Cargo.lock`。检查 diff 并运行：

```powershell
npm run check
```

3. 提交并推送版本变更：

```powershell
git add package.json package-lock.json Cargo.toml Cargo.lock
git commit -m "chore: prepare v<version>"
git push origin main
```

4. 在 `.git/release-notes-v<version>.md` 编写简短 Release Notes，避免把临时文件提交到仓库。第一行是 Tag 标题，其后必须有非空正文：

```markdown
codexhost v<version>

codexhost 是一个将 Pi 等 Harness 接入 Codex Desktop 的跨平台 Host，目前处于早期开发和 MVP 验证阶段。

本版本主要包含……

已知限制：……
```

5. 在刚推送的版本提交上创建并推送 annotated Tag：

```powershell
git tag -a v<version> -F .git/release-notes-v<version>.md
git show --no-patch v<version>
git push origin v<version>
```

6. 跟踪 `.github/workflows/release-packages.yml` 到完成，并核对：

- Windows x64/arm64 EXE 和 macOS x64/arm64 DMG 均已上传。
- 四个平台 npm tarball 和一个 `@codexhost/cli` 元包 tarball 均已上传。
- npm publish job 成功。
- GitHub Release 为正确的 Prerelease 或正式 Release，正文来自 Tag，附件共 9 个。

## 失败处理

- 构建或最终发布失败：先诊断原因，再重跑失败 jobs。
- npm 已接受某个版本后，不得修改内容并重新发布同一版本。
- 不得强制移动、删除或覆盖已推送 Tag；确需处理时先取得用户明确确认。
- 不得提交凭据、构建产物、日志、`image.png`、`nul` 或其他无关文件。
